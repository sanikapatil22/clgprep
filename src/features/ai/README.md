AI features are intentionally deferred for the MVP.

CampusLabs first ships the deterministic learning engine, progression system,
course/module/topic model, auth, and content ingestion plumbing. AI generation
can later consume the same topic block schema instead of introducing a separate
content format.
